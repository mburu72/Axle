package com.tumi.haul.service.kafka;
/*
import org.apache.kafka.clients.admin.NewTopic;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaTopicConfig {
    @Bean
    public NewTopic jobQuoteTopic(){
        return new NewTopic("quote-alert", 1, (short) 1);
    }
    @Bean
    public NewTopic quoteStatusTopic(){
        return new NewTopic("quote-status", 1, (short) 1);
    }
}
*/