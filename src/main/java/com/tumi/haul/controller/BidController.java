package com.tumi.haul.controller;

/*
@RestController
@RequestMapping("/api/bids")
public class BidController {
    @Autowired
    private BidService bidService;

    @PostMapping
    public Optional<Bid> createBid(@RequestBody Bid bid){
        return bidService.createBid(bid);
    }
    @DeleteMapping("/{id}")
    public ResponseEntity<Void>deleteBid(@PathVariable IdChar id, Bid bid){
        bidService.deleteBid(bid);
        return ResponseEntity.noContent().build();
    }
    @GetMapping
    public List<Bid> getAllBids(){
        return bidService.getAllBids();
    }
    @GetMapping("/{id}")
    public Optional<Bid>getBidById(@PathVariable IdChar id){
        return bidService.getBidById(id);
    }
    @GetMapping("/{job}")
    public Optional<Bid>getBidByJob(@PathVariable Job job){
        return bidService.getBidByJob(job);
    }
}
*/